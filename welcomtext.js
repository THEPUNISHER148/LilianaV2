const welcome = (number, groupname) => {
    return `Hola @${number}. Bienvenido al grupo ${groupname} Favor de pasarsela bien y respetar las reglas`
}
exports.welcome = welcome

const bye = (number) => {
    return `Hasta luego @${number}. jsjs`
}
exports.bye = bye
